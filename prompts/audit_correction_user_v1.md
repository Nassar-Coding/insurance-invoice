# Audit correction instruction v1

This is a retained summary of the user's actual correction-stage instruction, not a verbatim API prompt or invented model transcript.

The supplied independent implementation audit returned PASS WITH CORRECTIONS with exactly two substantive findings: a Major uncertainty-handling defect and a Moderate traceability defect. Inspect the audit, code, data, tests and outputs before accepting either finding. Correct the warranted defects; preserve a clear before/after record and explain any qualified acceptance or rejection with evidence. Keep the selected LLM contract-to-schema with deterministic pricing solution and governing plan.

Rerun the full existing suite, clean replay, Hospital 1 evaluation, prediction counts, submission validation, confidence/uncertainty behavior, traceability and affected execution/quality gates. Report every changed metric, count, confidence, category, omission or submission row. Verify unchanged outputs rather than assuming them. Regenerate all implementation outputs from the corrected state.

Return a consistent corrected project checkpoint ZIP, implementation_report.md and freshly generated submission.csv, even if the CSV is identical. Only declare CORRECTIONS COMPLETE - READY FOR INDEPENDENT CLOSURE RE-AUDIT after both findings are resolved or defensibly rejected and corrected execution/regression is complete. Do not publish, send anything, prepare final assessor presentation or perform publishing cleanup. Subsequent Continue messages reaffirm execution. The earlier explicit removal of time constraints remains in force.
