# Publishing preparation instruction record — v1

Source: the user's publishing-preparation instruction in this Work conversation.
This is a faithful scope record, not a claimed programmatic API transcript.

Use the exact audited checkpoint, corrected implementation report, 340-row
submission and independent closure re-audit together as the baseline. Inspect
and verify them before preparation. Preserve the accepted technical state; do
not alter code semantics, pricing, schemas, mappings, confidence, uncertainty or
predictions. A needed technical change requires stopping and reporting first.

Organize one repository-ready project; remove only nonessential temporary/debug
material while preserving prompt/decision history, audit evidence and reproducibility.
Finalize the README, dependencies and required reports truthfully. Execute a
clean-clone reproduction and full tests, and compare the generated CSV with the
exact attached reference. Keep actual execution evidence. Bounded agents may
assist, with the primary assistant responsible for reconciliation.

Prepare a project package, final README, reproduction result, exclusions and
remaining user actions. Do not create/push an external repository, grant account
access or send email. Later publishing/delivery requires separate authorization.
The final status must distinguish completed preparation from those pending actions.

This record is outside `prompts/` because that directory's existing bytes form
part of the frozen independently audited release manifest. The new instruction
governs packaging only; it does not reinterpret any contract.
